<% content_for :title, "#{t('blog.title')} · PhoneRelax" %>
<% content_for :description, t('blog.meta_description') %>
<div class="container section">
  <h1><%= t('blog.title') %></h1>
  <div class="grid">
    <%= render partial: 'blog/card', collection: @posts, as: :post %>
  </div>
</div>
