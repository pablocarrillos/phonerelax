# Copia este fichero a config/local_env.yml y rellena tus claves (NO se sube a git).
STRIPE_SECRET_KEY: sk_test_xxx
STRIPE_WEBHOOK_SECRET: whsec_xxx

# Envío de correo por SendGrid (SMTP). API key con permiso "Mail Send".
# El remitente info@phonerelax.com debe estar verificado en SendGrid.
SENDGRID_API_KEY: SG.xxxxxxxx
EMAIL_FROM: PhoneRelax <info@phonerelax.com>

# Solo para producción (en dokku va como variable de entorno, no en este fichero):
# ROLLBAR_ACCESS_TOKEN: token_de_rollbar
