#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Sin --ensure-latest: que una release nueva de Brakeman no rompa el CI;
# se actualiza a mano con `bundle update brakeman` de vez en cuando.
load Gem.bin_path("brakeman", "brakeman")
